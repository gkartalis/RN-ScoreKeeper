import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  sideContainer: {
    flex: 1.5,
    backgroundColor: '#000000',
    borderLeftColor: '#254b5d',
    borderLeftWidth: 2,
    marginBottom: 15
  }
});

export default styles;
