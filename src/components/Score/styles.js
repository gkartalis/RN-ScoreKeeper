import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 25,
    margin: 10
  },
  scoreText: {
    color: '#ffffff',
    fontSize: 55,
    fontWeight: 'bold'
  }
});

export default styles;
